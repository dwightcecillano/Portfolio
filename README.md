Dwight Cecillano — Portfolio

This repo contains a single-file portfolio (index.html) with assets.

To publish:
1. Create a remote GitHub repo and add it as `origin`.
2. `git push -u origin main`

Hosting options:
- GitHub Pages (free) — repo settings → Pages
- Netlify / Vercel — connect the GitHub repo and deploy
